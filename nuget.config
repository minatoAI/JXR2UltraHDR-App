<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- Restore packages to project-level directory so vcxproj can find them -->
    <!-- Relative to this config file's location (solution root) -->
    <add key="repositoryPath" value="JXR2UltraHDR.WinUI\packages" />
  </config>
</configuration>
